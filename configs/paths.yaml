# Paths are relative to the repository root.
# Large data, derived arrays, checkpoints, and external code live outside Git.

raw_mayo_dir: ../Mayo
train_dir: ../Mayo/train
test_dir: ../Mayo/test

derived_root: ../derived
processed_dir: ../derived/processed
degraded_dir: ../derived/degraded

checkpoint_dir: ../checkpoints
unet_checkpoint_dir: ../checkpoints/unet
diffpir_checkpoint_dir: ../checkpoints/diffpir

external_dir: ../external
diffpir_external_dir: ../external/diffpir

output_dir: outputs
figures_dir: outputs/figures
metrics_dir: outputs/metrics
reconstructions_dir: outputs/reconstructions
