# Paths are relative to the repository root.
# Large data, processed tensors, trained weights, and external code live outside Git.

raw_mayo_dir: ../Mayo
train_dir: ../Mayo/train
test_dir: ../Mayo/test

processed_dir: ../processed
degraded_dir: ../processed

weights_dir: ../weights
unet_weights_dir: ../weights/unet
diffpir_weights_dir: ../weights/diffpir

external_dir: ../external
diffpir_external_dir: ../external/diffpir
ippy_dir: ../IPPy

output_dir: outputs
figures_dir: outputs/figures
metrics_dir: outputs/metrics
reconstructions_dir: outputs/reconstructions
homework_weights_dir: ../weights/hw2
